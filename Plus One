class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        s=""
        for i in digits:
            s=s+str(i)
        a=int(s)+1
        l=[]
        for i in str(a):
            l.append(int(i))
        return l
